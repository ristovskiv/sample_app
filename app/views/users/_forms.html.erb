

		<%= fu.label :name%>
		<%= fu.text_field :name%>

		<%= fu.label :email%>
		<%= fu.text_field :email%>

		<%= fu.label :password%>
		<%= fu.password_field :password%>

		<%= fu.label :password_confirmation, "Confirmation"%>
		<%= fu.password_field :password_confirmation%>

		<%= fu.submit "Create my account", class: "btn btn-large btn-primary"%>